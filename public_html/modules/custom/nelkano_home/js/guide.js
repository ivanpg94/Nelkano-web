document.querySelectorAll('[data-r-lightbox]').forEach(link=>{link.addEventListener('click',event=>{const dialog=document.querySelector('.r-lightbox');if(!dialog?.showModal)return;event.preventDefault();dialog.querySelector('img').src=link.href;dialog.querySelector('img').alt=link.querySelector('img').alt;dialog.showModal();});});const lightbox=document.querySelector('.r-lightbox');lightbox?.querySelector('button').addEventListener('click',()=>lightbox.close());lightbox?.addEventListener('click',e=>{if(e.target===lightbox)lightbox.close();});