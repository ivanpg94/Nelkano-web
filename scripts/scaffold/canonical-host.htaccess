# Nelkano production canonical host; leave localhost and other development hosts alone.
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{HTTP_HOST} ^www\.nelkano\.com(?::[0-9]+)?$ [NC]
  RewriteRule ^ https://nelkano.com%{REQUEST_URI} [R=301,L,NE]
</IfModule>
